{
    "servers": {
        "full-auto-ci": {
            "command": "full-auto-ci",
            "args": [
                "mcp",
                "serve",
                "--stdio",
                "--no-token"
            ],
            "cwd": "${workspaceFolder}"
        }
    }
}